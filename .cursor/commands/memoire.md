Charge la memoire persistante du projet depuis le vault Obsidian (Cortex).

VAULT = `/Users/okatech/Documents/Obsidian Vault` — troisieme racine de cet espace
de travail, et serveur MCP `cortex-obsidian`.
Slug de ce projet : **`identite-ga`**. Projet consommateur : `digital-gabon`.

Lis `10-PROJETS/identite-ga/` : `contexte-actif.md`, `avancement.md`,
`fiche.md` (dont les **gotchas**), `architecture.md`, puis `decisions/`.
Ajoute `30-TRANSVERSE/chantiers.md` (chantiers ouverts) et
`00-GLOBAL/gotchas-transverses.md`.

⚠️ **Attention aux deux series d'ADR de ce depot**, qui se recouvrent :
`docs/adr/0001-oidc-architecture.md` d'un cote, `ressources/doc/adr-0001…0011` de
l'autre. Un numero seul est ambigu : citer aussi le titre.

## Puis fais une chose essentielle

**Confronte a `git log` tout ce que la memoire affirme de « livre / merge /
publie ».** Signale-moi chaque ecart entre la memoire et l'etat reel du depot —
c'est plus utile que le resume lui-meme.

Rends : le focus courant en 5 lignes, les chantiers ouverts, les gotchas qui
touchent ce que je vais faire, et la liste des ecarts.
